{
  "version": 2,
  "builds": [
    { "src": "api/vercel-api.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/vercel-api.js" }
  ],
  "env": {
    "JWT_SECRET": "@jwt-secret"
  }
}
